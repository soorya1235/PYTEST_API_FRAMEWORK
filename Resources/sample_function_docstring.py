"""
This Module has the sample function module docstirng
"""


def add(a, b):
    """
    Adds two numbers together.

    Parameters:
        a (int): The first integer operand.
        b (int): The second integer operand.

    Returns:
        int: The sum of `a` and `b`.
    """
    return a + b


print(sum(10, 20))
